/**
 * Questo file contiene le variabili di ambiente per la produzione.
 * A differenza dell'ambiente di sviluppo, qui specificheremo:
 * - URL dell'API in produzione
 * - Configurazioni specifiche per la produzione
 * - Flag che indica che siamo in ambiente di produzione
 */
export const environment = {
  production: true,
  apiUrl: 'https://api.orto-protesi.com/api',
};
